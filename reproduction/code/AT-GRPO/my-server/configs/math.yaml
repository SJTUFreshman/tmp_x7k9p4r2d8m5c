extends: _base.yaml
task: math
run_id: math_v2
rollout: {max_new_tokens: 1536}
train: {max_seq_length: 4096}
# Run the full pool directly; no complete prescreen allowlist is available.
pool: {prescreen: false, keep_band: [1, 7]}
# Zero disables the wall-clock stop. The run still finishes after 120 iterations.
budget: {max_iterations: 120, max_wall_clock_hours: 0.0}
