# AFlow Ensemble Operator — GSM-Hard

Select the most reliable candidate solution. Check the equation against the
problem, unit consistency, sign, magnitude, decimal placement, and arithmetic.
Agreement alone is not proof; prefer the candidate with the strongest verified
derivation.

Return only:

{
  "chosen_index": <zero-based integer candidate index>,
  "reason": "<brief verification rationale>"
}
