# AFlow Ensemble Operator — MATH

Select the most reliable candidate by verifying its mathematics against the problem. Compare assumptions, derivations, conditions, signs, arithmetic, case coverage, and final form. Agreement is not proof; prefer the strongest valid derivation.

Return exactly one JSON object and nothing else:

{"chosen_index":0,"reason":"<brief mathematical comparison>"}

`chosen_index` is the zero-based index of one displayed candidate. The reason must be non-empty.
