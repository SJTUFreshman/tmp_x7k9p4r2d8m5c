#!/usr/bin/env bash

PROJECT_ROOT="${PROJECT_ROOT:-/data/wangyuheng/jca}"
PYTHON_BIN="${PYTHON_BIN:-/data/conda_envs/qwen35/bin/python}"
PYTHONPATH_ROOT="${PYTHONPATH_ROOT:-/data/wangyuheng}"
ACCELERATE="${ACCELERATE:-/data/conda_envs/qwen35/bin/accelerate}"
VLLM_PYTHON_BIN="${VLLM_PYTHON_BIN:-/data/conda_envs/deep_research/bin/python}"
VLLM_LD_LIBRARY_PATH="${VLLM_LD_LIBRARY_PATH:-/data/conda_envs/deep_research/lib}"

MODEL_A1="${MODEL_A1:-/data/wangyuheng/models/Qwen3-1.7B}"
MODEL_A2="${MODEL_A2:-/data/wangyuheng/models/Qwen3-4B}"
MODEL_A3="${MODEL_A3:-/data/wangyuheng/models/Qwen3-8B}"
DATA_DIR="${DATA_DIR:-musique_data}"

SFT_A1="${SFT_A1:-${PROJECT_ROOT}/sft_runs/0706v2/A1/final}"
SFT_A2="${SFT_A2:-${PROJECT_ROOT}/sft_runs/0706v2/A2/final}"
SFT_A3="${SFT_A3:-${PROJECT_ROOT}/sft_runs/0706v2/A3/final}"

MAIN_ROLLOUT="${MAIN_ROLLOUT:-${PROJECT_ROOT}/rl_data/rl_14b_qwen_0716.jsonl}"
A3_ROLLOUT="${A3_ROLLOUT:-${PROJECT_ROOT}/rl_data/rl_14b_qwen_0716_a3confirm.jsonl}"

ROLLOUT_SPLIT="${ROLLOUT_SPLIT:-train}"
ROLLOUT_START="${ROLLOUT_START:-0}"
ROLLOUT_LIMIT="${ROLLOUT_LIMIT:-1000}"
ROLLOUT_T_MAX="${ROLLOUT_T_MAX:-8}"
ROLLOUT_START_AGENT="${ROLLOUT_START_AGENT:-A1}"
ROLLOUT_NUM_SAMPLES="${ROLLOUT_NUM_SAMPLES:-2}"
ROLLOUT_MAX_NEW_TOKENS="${ROLLOUT_MAX_NEW_TOKENS:-1024}"
ROLLOUT_TEMPERATURE="${ROLLOUT_TEMPERATURE:-0.9}"
ROLLOUT_TOP_P="${ROLLOUT_TOP_P:-0.95}"
ROLLOUT_CONCURRENCY="${ROLLOUT_CONCURRENCY:-2}"
REWARD_ALPHA="${REWARD_ALPHA:-0.6}"

QWEN_JUDGE_MODEL="${QWEN_JUDGE_MODEL:-/data/wangyuheng/models/Qwen3-14B}"
QWEN_JUDGE_NAME="${QWEN_JUDGE_NAME:-qwen14b_judge}"
QWEN_JUDGE_GPUS="${QWEN_JUDGE_GPUS:-0,1,2,3,4,5,6,7}"
QWEN_JUDGE_TP="${QWEN_JUDGE_TP:-8}"
QWEN_JUDGE_PORT="${QWEN_JUDGE_PORT:-8300}"
QWEN_JUDGE_CONCURRENCY="${QWEN_JUDGE_CONCURRENCY:-4}"
QWEN_JUDGE_MAX_TOKENS="${QWEN_JUDGE_MAX_TOKENS:-2048}"
QWEN_JUDGE_TEMPERATURE="${QWEN_JUDGE_TEMPERATURE:-0.0}"

KL_COEF="${KL_COEF:-0.2}"
NUM_EPOCHS="${NUM_EPOCHS:-2}"
LR="${LR:-1e-5}"
SEED="${SEED:-42}"
PER_DEVICE_BATCH="${PER_DEVICE_BATCH:-1}"
GRAD_ACCUM="${GRAD_ACCUM:-2}"
MAX_SEQ="${MAX_SEQ:-4096}"
WARMUP_RATIO="${WARMUP_RATIO:-0.05}"
WEIGHT_DECAY="${WEIGHT_DECAY:-0.01}"
LORA_RANK="${LORA_RANK:-64}"
LORA_ALPHA="${LORA_ALPHA:-64}"
LORA_DROPOUT="${LORA_DROPOUT:-0.05}"
NUM_GPUS="${NUM_GPUS:-8}"
MIXED_PRECISION="${MIXED_PRECISION:-bf16}"
LOGGING_STEPS="${LOGGING_STEPS:-10}"
SAVE_STEPS="${SAVE_STEPS:-100}"
SAVE_TOTAL_LIMIT="${SAVE_TOTAL_LIMIT:-3}"
REPORT_TO="${REPORT_TO:-tensorboard}"

EVAL_SPLIT="${EVAL_SPLIT:-dev}"
EVAL_START="${EVAL_START:-0}"
EVAL_LIMIT="${EVAL_LIMIT:-2417}"
EVAL_T_MAX="${EVAL_T_MAX:-8}"
EVAL_START_AGENT="${EVAL_START_AGENT:-A1}"
EVAL_MAX_NEW_TOKENS="${EVAL_MAX_NEW_TOKENS:-2048}"
EVAL_TEMPERATURE="${EVAL_TEMPERATURE:-0.0}"
EVAL_TOP_P="${EVAL_TOP_P:-0.95}"
EVAL_MAX_CONCURRENCY="${EVAL_MAX_CONCURRENCY:-64}"
EVAL_MAX_MODEL_LEN="${EVAL_MAX_MODEL_LEN:-8192}"

ABLATION_RUNS_ROOT="${ABLATION_RUNS_ROOT:-${PROJECT_ROOT}/rl_runs/ablation/musique}"
ABLATION_LOGS_ROOT="${ABLATION_LOGS_ROOT:-${PROJECT_ROOT}/logs/ablation/musique}"
ABLATION_DATA_ROOT="${ABLATION_DATA_ROOT:-${PROJECT_ROOT}/rl_data/ablation/musique}"
